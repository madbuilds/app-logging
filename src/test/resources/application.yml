app:
  logging:
    level:
      root: warn
      com.github.mad: info

    filter:
      duplicate-message-filter:
        enabled: true
        markers:
          FILTERED
          EXAMPLE
        repeats: 3
        expire: 5
        cache-size: 100

    appender:
      console: # prints to console
        enabled: true
        layout: json
      file: # prints to file
        enabled: false
        name: app.log
        layout: default
      loki: # prints to loki endpoint
        enabled: false
        name: TEST_APP